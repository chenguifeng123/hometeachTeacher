<view class="page">


<view class="page__bd">
<view class="weui-cells weui-cells_after-title">
            <view class="weui-cell" wx:for="{{classList}}" wx:key = "{{item.classId}}"  data-class="{{item.classId}}"  bindtap="jumpOneClass">
                <view class="weui-cell__bd">
                    <!--view>班级 : {{item.className}} 地址 :  {{item.address}}</view-->
                    <view class="desc">Class Name : {{item.className}}</view>
                </view>
            </view>
</view>


</view>
</view>